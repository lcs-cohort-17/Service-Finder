# 🚀 Team Onboarding – Service Finder

> Everything you need to get started in 10 minutes.

---

## 1. Clone the Repository

```bash
git clone https://github.com/lcs-cohort-17/Service-Finder
cd Service-Finder

# Always branch from dev
git checkout dev
git pull origin dev
git checkout -b your-name/dev/FE-001

# First push
git push -u origin HEAD

# Daily commits
git add .
git commit -m "feat: what you changed"
git push