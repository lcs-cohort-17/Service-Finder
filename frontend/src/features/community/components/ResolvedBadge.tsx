/*
Small badge that appears on resolved report cards.
 */
export function ResolvedBadge() {
  return (
    <span className="inline-block px-2 py-1 text-xs font-semibold text-gray-600 bg-gray-200 rounded-full">
      Resolved
    </span>
  );
}